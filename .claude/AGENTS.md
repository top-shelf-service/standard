# Agent Entry (Claude-Compatible Location)

Primary agent policy source is:

- `AGENTS.md`

Skill definitions and machine contracts:

- `.github/skills/`
- `ai-agent/`

Canonical executable domains:

- `scripts/agents/INDEX.md`

This file exists for tooling that looks for agent docs in `.claude/`.
